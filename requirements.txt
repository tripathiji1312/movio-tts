# Core runtime (CPU-safe install works; heavy deps load lazily at runtime)
numpy>=1.26,<3.0
PyYAML>=6.0
soundfile>=0.12
librosa>=0.10
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
redis>=5.0
httpx>=0.27
psutil>=5.9

# Acoustic stack (GPU). On Kaggle/CUDA machines these resolve to CUDA wheels.
torch>=2.2
transformers>=4.44
accelerate>=0.33
vocos>=0.1.0

# Dev
pytest>=8.0
