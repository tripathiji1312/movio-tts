# Research Index

- `research/findings-2026-09-04.md` — 2026-09-04 — Enable live/incremental streaming for movio IndicF5 TTS (fake WS streaming → per-chunk synthesize_stream + 12/euler fast corner + readiness/cancel/rate fixes); 5 parallel aspects + signature gate + plan-reviewer stress test, full verbatim logs. Update same day: CPU-first lock (12×Euler/sway-1/cfg2/cpu, 8 edge / 16 fallback / 24-midpoint slowest, threading/memory + naturalness gate), 3 focused subagents appended.
